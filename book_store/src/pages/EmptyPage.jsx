import React from 'react';

function EmptyPage() {
    return (
        <div className='p-about-container'>No items found, try a new search with another keywords (ui, node, new, php...)</div>
    );
}

export default EmptyPage;
